diff --git a/node_modules/@mui/system/createStyled.js b/node_modules/@mui/system/createStyled.js
index 1b72501..0fb4611 100644
--- a/node_modules/@mui/system/createStyled.js
+++ b/node_modules/@mui/system/createStyled.js
@@ -111,11 +111,11 @@ function createStyled(input = {}) {
     const skipVariantsResolver = inputSkipVariantsResolver !== undefined ? inputSkipVariantsResolver : componentSlot && componentSlot !== 'Root' || false;
     const skipSx = inputSkipSx || false;
     let label;
-    if (process.env.NODE_ENV !== 'production') {
+    
       if (componentName) {
         label = `${componentName}-${lowercaseFirstLetter(componentSlot || 'Root')}`;
       }
-    }
+    
     let shouldForwardPropOption = shouldForwardProp;
     if (componentSlot === 'Root') {
       shouldForwardPropOption = rootShouldForwardProp;
diff --git a/node_modules/@mui/system/esm/createStyled.js b/node_modules/@mui/system/esm/createStyled.js
index 839ad5e..a148d0d 100644
--- a/node_modules/@mui/system/esm/createStyled.js
+++ b/node_modules/@mui/system/esm/createStyled.js
@@ -100,11 +100,11 @@ export default function createStyled(input = {}) {
     const skipVariantsResolver = inputSkipVariantsResolver !== undefined ? inputSkipVariantsResolver : componentSlot && componentSlot !== 'Root' || false;
     const skipSx = inputSkipSx || false;
     let label;
-    if (process.env.NODE_ENV !== 'production') {
+    
       if (componentName) {
         label = `${componentName}-${lowercaseFirstLetter(componentSlot || 'Root')}`;
       }
-    }
+    
     let shouldForwardPropOption = shouldForwardProp;
     if (componentSlot === 'Root') {
       shouldForwardPropOption = rootShouldForwardProp;
diff --git a/node_modules/@mui/system/legacy/createStyled.js b/node_modules/@mui/system/legacy/createStyled.js
index ca8d651..a130401 100644
--- a/node_modules/@mui/system/legacy/createStyled.js
+++ b/node_modules/@mui/system/legacy/createStyled.js
@@ -102,11 +102,11 @@ export default function createStyled() {
     var skipVariantsResolver = inputSkipVariantsResolver !== undefined ? inputSkipVariantsResolver : componentSlot && componentSlot !== 'Root' || false;
     var skipSx = inputSkipSx || false;
     var label;
-    if (process.env.NODE_ENV !== 'production') {
+    
       if (componentName) {
         label = "".concat(componentName, "-").concat(lowercaseFirstLetter(componentSlot || 'Root'));
       }
-    }
+    
     var shouldForwardPropOption = shouldForwardProp;
     if (componentSlot === 'Root') {
       shouldForwardPropOption = rootShouldForwardProp;
diff --git a/node_modules/@mui/system/modern/createStyled.js b/node_modules/@mui/system/modern/createStyled.js
index 6cf3641..2630cae 100644
--- a/node_modules/@mui/system/modern/createStyled.js
+++ b/node_modules/@mui/system/modern/createStyled.js
@@ -99,11 +99,11 @@ export default function createStyled(input = {}) {
     const skipVariantsResolver = inputSkipVariantsResolver !== undefined ? inputSkipVariantsResolver : componentSlot && componentSlot !== 'Root' || false;
     const skipSx = inputSkipSx || false;
     let label;
-    if (process.env.NODE_ENV !== 'production') {
+    
       if (componentName) {
         label = `${componentName}-${lowercaseFirstLetter(componentSlot || 'Root')}`;
       }
-    }
+    
     let shouldForwardPropOption = shouldForwardProp;
     if (componentSlot === 'Root') {
       shouldForwardPropOption = rootShouldForwardProp;
