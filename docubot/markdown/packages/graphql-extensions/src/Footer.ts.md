Summary:
This file exports a GraphQL schema definition language (SDL) string that extends the Footer type with a navigationItems field of type Collection.

Import statements:
The file imports the gql function from the graphql-tag library.

Script Summary:
The typeDefs constant exports a GraphQL SDL string that extends the Footer type with a navigationItems field of type Collection.

Internal Functions:
There are no internal functions in this file.

External Functions:
There are no external functions in this file.

Interaction Summary:
This file defines the schema for the Footer type in the larger GraphQL schema of the application. The schema is used by the GraphQL server to validate and execute queries and mutations.

Developer Questions:
- What is the Collection type?
- How is the navigationItems field used in the application?
- Are there any other types that extend the Footer type?
- How are queries and mutations defined and executed in the application?
- How is the GraphQL server configured in the application?

Known Issues and Todo Items:
There are no known issues or bugs with this component. No todo items need to be addressed.